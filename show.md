### Show the full diff for a commit

```
git show <commit-hash>
```

### Show only the changed files

```
git show --name-only <commit-hash>
```

### Show changed files with summary

```
git show --stat <commit-hash>
```