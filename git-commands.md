git log --oneline --graph

git commit -am "Feature: personalize title"

git add --all - To add and commit files to a Git repository

## After fixing merge conflicts Correct sequence because the commit already exists
git add .
git rebase --continue